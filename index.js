/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import AnimTest from './AnimTest';
import Movement from './Movement';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Movement);
